const express = require("express")

module.exports = sellerDeleteItem

function sellerDeleteItem() {
    return async (req, res, next) => {
        try {
            
        } catch(err) {
            next(err)
        }
    }
}